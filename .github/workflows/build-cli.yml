name: Build CLI

on:
  push:
    branches: [main]
    paths:
      - 'tools/gins-rime'
      - 'tools/gins-rime-cli/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  upload-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Upload zsh script to R2
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          npm install -g wrangler
          wrangler r2 object put gins-rime/cli/gins-rime-latest \
            --file tools/gins-rime \
            --content-type "application/x-sh"
          SHA=$(git rev-parse --short HEAD)
          DATE=$(date +%Y%m%d)
          echo "{\"sha\":\"${SHA}\",\"date\":\"${DATE}\"}" > /tmp/cli-meta.json
          wrangler r2 object put gins-rime/cli/meta.json \
            --file /tmp/cli-meta.json \
            --content-type "application/json"

  build-swift:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Swift binary
        working-directory: tools/gins-rime-cli
        run: |
          swift build -c release
          cp .build/release/GinsRime /tmp/gins-rime-native
          chmod +x /tmp/gins-rime-native

      - name: Upload Swift binary to R2
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          npm install -g wrangler
          wrangler r2 object put gins-rime/cli/gins-rime-native \
            --file /tmp/gins-rime-native \
            --content-type "application/octet-stream"
